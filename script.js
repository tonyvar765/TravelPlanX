// JavaScript for handling form submission and other interactions
document.getElementById('itineraryForm').addEventListener('submit', function(e) {
    e.preventDefault();
    const destination = document.getElementById('destinationInput').value;
    const activity = document.getElementById('activityInput').value;
    // Code to handle adding destinations/activities to the itinerary list
  });
  
  document.getElementById('loginBtn').addEventListener('click', function() {
    // Code to handle login functionality
    // You might integrate with social media login APIs here
  });
  